WcfRestContrib.Tests

Note that the smoke tests programmatically use a 32-bit only dev web server.  Consequently this project needs to
be built 32-bit only.

Should contain:

- Smoke tests for WcfRestContrib (depends on NielsBohrLibrary)
- Any other unit tests related to WcfRestContrib

Should not contain:

- Production code